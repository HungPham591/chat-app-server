type Language{
    id: ID
    users: [User]
    code: Int
    name: String
    created: Date
    deleteAt: Date
}
type Query{
    language(id:ID!):Language
    languages:[Language]
}
type Mutation{
    createLanguage(name:String!):Language
    updateLanguage(id:ID!,name:String!):Language
}