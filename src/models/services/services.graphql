type Service{
    id: ID
    users: [User]
    code: Int
    title: String
    description: String
    price: Int
    created: Date
    deleteAt: Date
}
type Query{
    services:[Service]
}
# type Resolver{
    
# }